// SQL Interview mastery additions. Original joins and ranking lessons stay intact.
const makeQuiz = (question, options, answer, explanation) => ({ question, options, answer, explanation })
const lesson = ({ id, title, emoji, definition, hinglish, topics, code, question, options, answer, explanation, challenge, notes, interview }) => ({
  id, title, emoji, xpReward: 45, badgeName: 'SQL Interview Ace', readingTime: '14 min', difficulty: 'Beginner to Advanced',
  mastirQuote: { start: '👨‍🏫 Masti Sir: "Pehle result ki grain samjho—ek row kis cheez ko represent kar rahi hai? Phir query likho!"', hint: '🤦 Masti Sir: "Query chal jana correctness ka proof nahi; duplicates, NULL aur edge cases check karo."', success: '🥳 Masti Sir: "Query ka logic, performance aur explanation—teeno interview-ready!"', mistake: '😅 Masti Sir: "SELECT * aur accidental cross join se interviewer ko darao mat."' },
  englishDef: definition, hinglishExplain: hinglish,
  funnyExample: { scenario: `Interview focus:\n${topics.map(topic => `• ${topic}`).join('\n')}`, punchline: 'SQL is a story about rows—make every clause earn its place.' },
  visualDiagram: { title: `${title} — query reasoning flow`, type: 'flow', nodes: ['Define output row grain', 'Check schema + keys', 'Build smallest correct query', 'Test NULL / duplicate edge cases', 'Inspect cost + explain trade-off'] },
  code, commonMistakes: ['Forgetting that NULL is not equal to NULL—use IS NULL.', 'Filtering after a LEFT JOIN in WHERE and accidentally turning it into an INNER JOIN.', 'Using a non-sargable expression on an indexed filter column.'],
  miniChallenge: challenge, quiz: makeQuiz(question, options, answer, explanation), summary: notes,
  flashcard: { q: `What should you state first for ${title}?`, a: 'The output grain, required fields, and the edge cases that affect correctness.' }, interviewQuestions: interview
})

export const sqlInterviewMasteryChapters = [
  { chapterTitle: 'Chapter 3: SQL Fundamentals, Filtering & Aggregation', lessons: [lesson({ id: 'sql-int-fundamentals-aggregation', title: 'SQL Foundations, SELECT Filtering & Aggregation Interview Logic', emoji: '🧱', definition: 'Relational SQL queries transform rows into a result set. Keys and constraints preserve identity and relationships; filtering reduces rows before aggregation, while HAVING filters groups after aggregates are computed.', hinglish: 'WHERE bouncer hai jo rows ko party mein aane se pehle filter karta hai. HAVING party ke baad group ko filter karta hai. COUNT(column) NULL ko ignore karta hai; COUNT(*) row ko count karta hai.', topics: ['Keys, constraints, NULL, DEFAULT', 'SELECT, WHERE, IN, LIKE, EXISTS', 'GROUP BY, HAVING, COUNT(*) vs COUNT(column)', 'WHERE vs HAVING and output grain'], code: `-- Departments whose average active salary is above 60000\nSELECT department_id, COUNT(*) AS employee_count, AVG(salary) AS avg_salary\nFROM employees\nWHERE status = 'active'\nGROUP BY department_id\nHAVING AVG(salary) > 60000;`, question: 'Which clause filters groups after AVG or COUNT is calculated?', options: ['WHERE', 'HAVING', 'ORDER BY', 'DISTINCT'], answer: 'HAVING', explanation: 'WHERE filters rows before grouping; HAVING filters the computed groups.', challenge: 'Return each category with at least five in-stock products and its average price.', notes: ['Start every query by naming the output row grain.', 'WHERE precedes GROUP BY; HAVING follows it.', 'NULL changes comparison and count behavior.'], interview: ['COUNT(*) vs COUNT(order_id)?', 'Why can WHERE AVG(salary) > 50000 not work?'] })] },
  { chapterTitle: 'Chapter 4: Joins, Subqueries & CTEs', lessons: [lesson({ id: 'sql-int-joins-subqueries-ctes', title: 'Join Traps, Correlated Subqueries & CTEs', emoji: '🔗', definition: 'Joins combine related rows by a key; subqueries express dependent or set-based conditions; CTEs name intermediate relations to improve readability and isolate logic. Join cardinality determines whether duplicates are expected.', hinglish: 'JOIN se pehle relation ka cardinality bolo: one-to-many hai toh parent duplicate dikhega—ye bug nahi, expected result ho sakta hai. CTE query ko paragraphs mein todta hai.', topics: ['INNER / LEFT / SELF joins', 'Anti joins and NOT EXISTS', 'Correlated subqueries', 'CTEs and recursive CTE concept'], code: `-- Employees paid above their own department average\nWITH department_average AS (\n  SELECT department_id, AVG(salary) AS avg_salary\n  FROM employees GROUP BY department_id\n)\nSELECT e.name, e.salary\nFROM employees e\nJOIN department_average d ON d.department_id = e.department_id\nWHERE e.salary > d.avg_salary;`, question: 'Which pattern safely finds customers with no orders?', options: ['LEFT JOIN orders then WHERE orders.id IS NULL', 'INNER JOIN orders', 'ORDER BY orders.id', 'COUNT(*) without grouping'], answer: 'LEFT JOIN orders then WHERE orders.id IS NULL', explanation: 'The left join preserves every customer and the NULL identifies an absent matching order.', challenge: 'Find products never reviewed using NOT EXISTS, then explain why it avoids a NULL trap.', notes: ['Know whether a join can multiply rows.', 'NOT EXISTS is often clearer than NOT IN when NULL is possible.', 'CTEs improve reasoning; the optimizer may inline them depending on the database.'], interview: ['When does a LEFT JOIN become an accidental INNER JOIN?', 'CTE versus derived table?'] })] },
  { chapterTitle: 'Chapter 5: Windows, Dates, Strings & CASE', lessons: [lesson({ id: 'sql-int-windows-transformations', title: 'Window Functions, Dates, Strings, CASE & NULL Mastery', emoji: '🪟', definition: 'Window functions calculate over a related set of rows without collapsing them. Date, string, CASE, and NULL functions normalize data and express business logic while preserving the result grain.', hinglish: 'GROUP BY summary banata hai; window function har row ko rakhta hai aur uske side mein rank, running total ya previous row ki info chipka deta hai.', topics: ['ROW_NUMBER, RANK, DENSE_RANK', 'LAG, LEAD, running totals', 'Date boundaries and time zones', 'CASE, COALESCE, NULLIF'], code: `-- Revenue running total by customer\nSELECT customer_id, order_date, amount,\n  SUM(amount) OVER (PARTITION BY customer_id ORDER BY order_date, id) AS running_revenue,\n  LAG(amount) OVER (PARTITION BY customer_id ORDER BY order_date, id) AS previous_order\nFROM orders;`, question: 'What keeps all detail rows while adding a ranking?', options: ['A window function with OVER()', 'GROUP BY only', 'DISTINCT', 'HAVING'], answer: 'A window function with OVER()', explanation: 'Windows calculate across a partition without reducing it to one row per group.', challenge: 'Bucket orders as Small, Medium, or Large with CASE and compute a monthly running count.', notes: ['Choose a deterministic ORDER BY for windows.', 'COALESCE supplies a fallback; NULLIF can prevent divide-by-zero.', 'Time zones and inclusive date boundaries are interview edge cases.'], interview: ['RANK versus DENSE_RANK?', 'Why add id after order_date in a window ordering?'] })] },
  { chapterTitle: 'Chapter 6: Complex SQL Patterns & Business Analytics', lessons: [lesson({ id: 'sql-int-complex-business-patterns', title: 'Gaps & Islands, Retention, Duplicates & Business SQL', emoji: '📈', definition: 'Complex analytical SQL converts event records into sequences, cohorts, funnels, and business metrics. The key is defining the entity, time grain, and inclusion rules before composing windows or self joins.', hinglish: 'Gaps & islands ka secret dates nahi, sequence break hai. Har user ki continuous activity ko group id do; phir each island par aggregate karo.', topics: ['Gaps and islands', 'Consecutive activity', 'Deduplication', 'Cohort retention and funnels', 'Revenue, conversion, and inventory analysis'], code: `-- Keep one row per normalized email; flag the rest\nWITH marked AS (\n  SELECT id, ROW_NUMBER() OVER (\n    PARTITION BY LOWER(email) ORDER BY created_at, id\n  ) AS rn\n  FROM users\n)\nSELECT id FROM marked WHERE rn > 1;`, question: 'What must be defined before calculating a retention rate?', options: ['Cohort and activity window', 'Font family', 'Join keyword preference', 'Server RAM'], answer: 'Cohort and activity window', explanation: 'Retention is meaningless without a cohort start and a repeat-activity definition.', challenge: 'Write the data grain and numerator/denominator for a weekly buyer-retention query.', notes: ['Business metrics are definitions, not just formulas.', 'Deduplicate deliberately and preserve auditability.', 'Use small CTEs to validate each stage of a complex query.'], interview: ['How would you compute a conversion funnel?', 'How do you explain gaps and islands?'] })] },
  { chapterTitle: 'Chapter 7: Transactions, Indexing & Optimization', lessons: [lesson({ id: 'sql-int-performance-design', title: 'Transactions, Isolation, Indexes, SARGability & Query Optimization', emoji: '🚀', definition: 'Transactions group changes with atomicity and isolation. Indexes provide ordered access paths; query optimization compares alternatives based on data distribution and cost. A sargable predicate lets the engine use an index efficiently.', hinglish: 'Index bookshelf ka catalog hai—read fast hota hai, par every write pe catalog update bhi hota hai. Column par function lagaya toh engine ko har book kholni pad sakti hai.', topics: ['ACID and isolation anomalies', 'B-tree composite indexes', 'EXPLAIN plans', 'SARGable predicates', 'Locks, deadlocks, and safe retries'], code: `-- Less sargable: WHERE DATE(created_at) = '2026-08-22'\n-- Sargable range: index on created_at can seek\nSELECT * FROM orders\nWHERE created_at >= '2026-08-22'\n  AND created_at <  '2026-08-23';`, question: 'Which predicate is usually more index-friendly?', options: ['A range on the raw indexed timestamp', 'DATE(indexed_timestamp) = value', 'Casting every row', 'Leading wildcard LIKE'], answer: 'A range on the raw indexed timestamp', explanation: 'It allows a range seek instead of applying a function to every candidate row.', challenge: 'Propose an index for user order history sorted by latest date and explain its write cost.', notes: ['Use EXPLAIN and real selectivity; do not guess.', 'Index order should follow common filter and sort paths.', 'Transactions need idempotency and retry policies around deadlocks.'], interview: ['What is a covering index?', 'How do you diagnose a slow query?'] })] },
  { chapterTitle: 'Chapter 8: Security, Database Design & SQL vs NoSQL', lessons: [lesson({ id: 'sql-int-security-database-design', title: 'SQL Security, Normalization, Schema Design & SQL vs NoSQL', emoji: '🔐', definition: 'Database design models entities and constraints to preserve data quality. Normalization reduces update anomalies; security uses parameterized queries, least privilege, auditing, and controlled access. SQL and NoSQL are trade-offs, not rival teams.', hinglish: 'String concatenate karke query banana SQL injection ko invitation card dena hai. Parameter use karo. Normalization data ko clean rakhta hai; kabhi read-speed ke liye denormalization bhi intentional hota hai.', topics: ['Parameterized queries and least privilege', 'Primary / foreign keys and normalization', 'Denormalization trade-offs', 'SQL vs NoSQL workload choice', 'Schema evolution'], code: `-- Parameterized query: values are data, never executable SQL text\nSELECT id, email FROM users WHERE email = $1;\n\n-- A foreign key encodes a real invariant\nALTER TABLE orders\n  ADD CONSTRAINT orders_user_fk FOREIGN KEY (user_id) REFERENCES users(id);`, question: 'What is the primary defense against SQL injection?', options: ['Parameterized queries', 'Longer table names', 'More indexes', 'SELECT *'], answer: 'Parameterized queries', explanation: 'Parameters keep values separate from SQL syntax.', challenge: 'Model users, products, orders, and order_items with keys; state one normalization and one denormalization choice.', notes: ['Use constraints for invariants the database can enforce.', 'Give applications only the permissions they need.', 'Select SQL or NoSQL from access patterns, consistency, and operations.'], interview: ['When is denormalization worth it?', 'How do prepared statements prevent injection?'] })] },
  { chapterTitle: 'Chapter 9: SQL Interview Practice System', lessons: [lesson({ id: 'sql-int-practice-mock', title: 'Question Patterns, Debugging, Performance Scores & Mock Interviews', emoji: '🎯', definition: 'Interview practice combines query correctness, explanation, readability, and performance. A strong answer tests sample data and edge cases, states assumptions, then offers an alternative if the data size or database changes.', hinglish: 'Interview mein silent typing nahi. Pehle approach bolo, query ka grain bolo, phir sample case dry run karo. Wrong query se darna nahi—debugging ka process show karo.', topics: ['Output prediction and execution order', 'Query debugging', 'Easy → expert difficulty progression', 'Performance/readability feedback', 'Mock interview communication'], code: `-- SQL logical order (simplified)\nFROM / JOIN → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT\n\n-- Debug tactic: first run the join without aggregates, then count rows.`, question: 'What is a reliable first debugging step for a surprising aggregate?', options: ['Inspect the joined rows and their grain', 'Add random DISTINCT', 'Remove all filters', 'Rewrite in another language'], answer: 'Inspect the joined rows and their grain', explanation: 'Unexpected multiplication or filtering becomes visible before aggregation hides it.', challenge: 'Explain a solution to “highest paid employee in each department” aloud: assumptions, query, edge cases, and performance.', notes: ['A correct answer includes its assumptions and edge cases.', 'Use execution order to debug filters and aliases.', 'Practice explanations, not only final SQL text.'], interview: ['Give feedback on this query’s readability and index use.', 'How would you handle a follow-up requiring ties?'] })] },
  { chapterTitle: 'Chapter 10: E-commerce SQL Capstone & Final Assessment', lessons: [lesson({ id: 'sql-int-ecommerce-capstone', title: 'Capstone: E-commerce Insights, 50-Problem Roadmap & Final Assessment', emoji: '🏆', definition: 'The capstone combines an e-commerce schema—users, products, categories, orders, order items, payments, reviews, and shipments—into progressively harder analytics, operational, and optimization queries.', hinglish: 'Final challenge mein ek table nahi, poora business dekho: revenue, repeat buyers, low stock, delayed shipment, payment failures. Query ke result ko decision se connect karo.', topics: ['Schema-first problem solving', 'Business insights and data quality', 'Analyst, data-engineer, and backend SQL', 'Daily challenge and revision plan', 'Final mock interview'], code: `-- Monthly completed-order revenue, excluding refunded orders\nSELECT DATE_TRUNC('month', o.created_at) AS month,\n       SUM(oi.quantity * oi.unit_price) AS revenue\nFROM orders o\nJOIN order_items oi ON oi.order_id = o.id\nWHERE o.status = 'completed'\nGROUP BY 1\nORDER BY 1;`, question: 'Before reporting revenue, which business rule must be explicit?', options: ['Which order states count and how refunds are handled', 'The editor color', 'The SQL keyword casing', 'The browser width'], answer: 'Which order states count and how refunds are handled', explanation: 'A metric is only trustworthy when its inclusions and exclusions are defined.', challenge: 'Create a final insight report: monthly revenue, repeat buyers, top categories, refund rate, and late shipments—state all assumptions.', notes: ['Treat schemas and metric definitions as part of the answer.', 'Progress from a correct baseline to an indexed, explainable solution.', 'Use daily practice to revisit weak patterns such as windows or gaps-and-islands.'], interview: ['Design an order analytics query that scales.', 'Which dashboard metric would alert you to checkout failure?'] })] }
]
